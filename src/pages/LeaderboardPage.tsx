import { useEffect, useState } from 'react'; import { Link } from 'react-router-dom'; import { fetchLeaderboard, type Leader } from '../services/socialService'; import { useAppStore } from '../store/useAppStore';
export function LeaderboardPage() { const auth = useAppStore((state) => state.authStatus); const [entries, setEntries] = useState<Leader[]>([]); useEffect(() => { if (auth !== 'authenticated') return; const load = () => void fetchLeaderboard().then(setEntries); load(); const timer = window.setInterval(load, 5_000); return () => window.clearInterval(timer); }, [auth]); return <main className="page commerce-page"><Link className="brand" to="/">Lumos</Link><header className="commerce-header"><div><h1>Leaderboard</h1><p>Global ranking · refreshes every 5 seconds</p></div></header><div className="leaderboard">{entries.map((entry) => <div key={entry.userId}><b>#{entry.rank}</b><span>{entry.username}</span><strong>{entry.coins.toLocaleString()} MTX</strong></div>)}</div></main>; }

