import { useState } from 'react'; import { Navigate, useNavigate } from 'react-router-dom'; import { adminLogin } from '../services/adminService'; import { useAppStore } from '../store/useAppStore';
export function AdminLoginPage() { const authenticated = useAppStore((state) => state.adminAuthenticated); const setAuthenticated = useAppStore((state) => state.setAdminAuthenticated); const navigate = useNavigate(); const [username, setUsername] = useState(''); const [password, setPassword] = useState(''); const [otp, setOtp] = useState(''); const [error, setError] = useState(''); if (authenticated) return <Navigate to="/admin" replace />; const submit = async (event: React.FormEvent) => { event.preventDefault(); try { await adminLogin(username, password, otp); setAuthenticated(true); navigate('/admin'); } catch { setError('Admin authentication failed.'); } }; return <main className="admin-login"><form onSubmit={(event) => void submit(event)}><h1>MTX Admin</h1><p>Separate operator authentication</p><input autoComplete="username" placeholder="Username" value={username} onChange={(event) => setUsername(event.target.value)} /><input autoComplete="current-password" type="password" placeholder="Password" value={password} onChange={(event) => setPassword(event.target.value)} /><input inputMode="numeric" maxLength={6} placeholder="6-digit OTP" value={otp} onChange={(event) => setOtp(event.target.value)} /><button className="button primary" type="submit">Sign in</button>{error && <p className="payment-failed">{error}</p>}</form></main>; }

